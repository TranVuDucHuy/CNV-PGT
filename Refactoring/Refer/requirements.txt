# WisecondorX Python dependencies
numpy
pandas
matplotlib
scipy
scikit-learn==1.4.2
pysam

# Core utilities
pydantic

# CNV plugin interface (assumed published; pin version if known)
huycnv

# Notes:
# - WisecondorX must be installed from bioconda (not available on pip):
#   micromamba install -n wisecondorx-plugin wisecondorx -c bioconda -y
# - WisecondorX also requires R packages (jsonlite, DNAcopy) - included in bioconda install
# - scikit-learn pinned to 1.4.2 to match WisecondorX v1.4.2
# - Preferred: install from conda-forge/bioconda to avoid building from source
